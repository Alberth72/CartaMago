# Supabase project credentials
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Restaurant identifier — change this to switch between restaurants
# Each restaurant needs its own row in the 'restaurants' table and a seed entry
VITE_RESTAURANT_ID=brasas-sazon

# Storage bucket for menu images
VITE_MENU_STORAGE_BUCKET=menu-assets
