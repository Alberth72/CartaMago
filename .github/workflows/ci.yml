name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build (tsc + vite)
        run: npm run build

      - name: Unit tests + coverage (TDD)
        run: npm run test:coverage

      - name: Install Playwright browsers (chromium)
        run: npx playwright install --with-deps chromium

      - name: E2E public menu (seed fallback)
        run: npm run test:e2e

      - name: E2E admin (mock)
        run: npm run test:e2e:admin

      - name: Coverage report por proceso (TDD + BDD)
        run: npm run coverage:e2e:convert && npm run coverage:report